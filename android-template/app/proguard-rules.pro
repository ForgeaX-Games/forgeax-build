# Add project-specific ProGuard rules here.
# The game runs entirely inside a WebView (assets/public/*), so no app-side
# JS-bridge classes need keeping by default. Add keep rules here if you wire
# a @JavascriptInterface bridge for third-party SDKs.
