pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

// Replaced at export time by AndroidPackager (androidProjectName).
rootProject.name = "__FORGEAX_PROJECT_NAME__"
include(":app")
