{
  "name": "@forgeax/build",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Build orchestrator: source repos -> forgeax monorepo (vendor + recipes + validators)",
  "scripts": {
    "release-source": "bash build.sh release-source",
    "validate": "bash build.sh validate",
    "clean": "bash build.sh clean",
    "sync-upstream": "bun scripts/sync-upstream.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3"
  }
}
